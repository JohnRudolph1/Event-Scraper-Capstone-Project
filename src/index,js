const { default: axios } = require("axios");


window.addEventListener ("load", (event) => {
    const allButton = document.querySelector("#scrape");

    allButton.addEventListener("click", fetch("http://localhost:4200/scrape"));
});
